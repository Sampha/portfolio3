<%= form_for(photo_catagory) do |f| %>
  <% if photo_catagory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo_catagory.errors.count, "error") %> prohibited this photo_catagory from being saved:</h2>

      <ul>
      <% photo_catagory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
